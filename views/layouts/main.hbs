<!DOCTYPE html>
<html lang="en" prefix="og: http://ogp.me/ns#">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    {{#if openGraph}}
        <meta property="og:title" content="{{ openGraph.title }}">
        <meta property="og:type" content="{{ openGraph.type }}">
        <meta property="og:url" content="{{ openGraph.url }}">
        {{#if openGraph.image}}
            <meta property="og:image" content="{{ openGraph.image }}">
        {{else}}
            <meta property="og:image" content="/static/images/me_alternate.jpg">
        {{/if}}
        <meta property="og:description" content="{{ openGraph.description }}">
    {{/if}}
    <!-- Navbar Stylesheet -->
    <link rel="stylesheet" href="/static/css/navbar.css">
    <!-- Card Stylesheet -->
    <link rel="stylesheet" href="/static/css/card.css">
    <!-- Form Stylesheet -->
    <link rel="stylesheet" href="/static/css/form.css">
    <!-- Footer Stylesheet -->
    <link rel="stylesheet" href="/static/css/footer.css">
    <!-- Main Stylesheet -->
    <link rel="stylesheet" href="/static/css/style.css">
    {{#if css}}
        {{#each css}}
            <link rel="stylesheet" href="{{this}}">
        {{/each}}
    {{/if}}
    <!-- Favicon -->
    <link rel="shortcut icon" href="/static/images/me_alternate_favicon.jpg" type="image/jpg">
    <title>{{ title }}</title>
</head>
<body>
    {{> navbar}}
    {{{ body }}}
    {{#if js}}
        {{#each js}}
        <script src="{{this}}"></script>
        {{/each}}
    {{/if}}
    <!-- UI Functions JS File -->
    <script src="/static/js/ui-functions.js"></script>
    <!-- Main JS File -->
    <script src="/static/js/main.js"></script>
</body>
</html>
